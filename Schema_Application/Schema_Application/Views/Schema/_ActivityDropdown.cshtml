@model IEnumerable<Schema_Application.ViewModels.ActivityViewModel>

<select id="activity">
    @foreach(var activity in Model)
    {
        <option value="@activity.ActivityId">
            @activity.Name
        </option>
    }
</select>